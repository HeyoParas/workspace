function displayQuestionForDiscussion(target)
{
    for(let i=0;i<arr.length;i++)
    {
        if(target.id===arr[i].id)
        {
            let temp=JSON.parse(localStorage.getItem('questions')) || [];
            displayQuestion.appendChild(tem)
        }
    }
}